export type LocatorHint = {
    id: string;
    title: string;
    description?: string;
    selectors: string[];
    purpose: string;
    actions: string[];
    docsUrl: string;
    target: string;
};
